[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-llm-starter"
version = "0.1.0"
description = "Minimal hands-on Agent LLM project with tools and local/hosted backends"
authors = [{name = "You"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["src/agent_llm_starter"]
